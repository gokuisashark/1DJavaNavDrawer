<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fish.a1djava">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".NavigationViewActivity"
                  android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".HomePage" />
        <activity android:name=".ForgetPassword" />
        <activity android:name=".Register" />
        <activity android:name=".ShowAlgo"/>
        <activity android:name=".ShowCs" />
        <activity android:name=".ShowJava" />
        <activity
            android:name=".calendar.BasicActivity"
            android:label="Calendar" >
        </activity>

        <activity
            android:name=".calendar.Consultation"
            android:label="Consultation" >
        </activity>

        <activity
            android:name=".calendar.Professor_Consultation"
            android:label="Professor_Consultation" >
        </activity>

    </application>

</manifest>